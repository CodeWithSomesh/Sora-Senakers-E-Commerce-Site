╔══════════════════════════════════════════════════════════════════════════╗
║                    AUTH0 ACTION SETUP - QUICK GUIDE                      ║
╚══════════════════════════════════════════════════════════════════════════╝

This guide shows you how to install the Auth0 Action that checks your
database before allowing logins. This is ESSENTIAL for the blocking system
to work properly.

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Open Auth0 Dashboard                                            │
└──────────────────────────────────────────────────────────────────────────┘

URL: https://manage.auth0.com/
Login with your Auth0 account

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Navigate to Actions                                             │
└──────────────────────────────────────────────────────────────────────────┘

Left Sidebar → Actions → Library

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Create Custom Action                                            │
└──────────────────────────────────────────────────────────────────────────┘

Click: [+ Build Custom] button (top right)

Fill in:
  Name: Check Database Block Status
  Trigger: Login / Post Login
  Runtime: Node 18 (recommended)

Click: [Create]

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Copy the Code                                                   │
└──────────────────────────────────────────────────────────────────────────┘

1. Open file: backend/AUTH0_ACTION_LOGIN_CHECK.js
2. Copy ALL the code (Ctrl+A, Ctrl+C)
3. Paste into Auth0 editor (replace default code)

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Add Secret                                                      │
└──────────────────────────────────────────────────────────────────────────┘

On the left sidebar, click the 🔒 lock icon (Secrets)

Click: [Add Secret]

  Key: API_BASE_URL
  Value: http://localhost:7000

  NOTE: For production, use your actual API URL
        Example: https://api.yoursite.com

Click: [Create]

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 6: Deploy the Action                                               │
└──────────────────────────────────────────────────────────────────────────┘

Click: [Deploy] button (top right)

Wait for "Deployed successfully" message ✓

┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 7: Add Action to Login Flow                                        │
└──────────────────────────────────────────────────────────────────────────┘

Navigate to: Actions → Flows → Login

You'll see a flow diagram:
  [Start] → → → [Complete]

On the RIGHT sidebar, find your action:
  "Check Database Block Status"

DRAG the action between [Start] and [Complete]:
  [Start] → [Check Database Block Status] → [Complete]

Click: [Apply] button (top right)

Confirm: Click [Apply] in the popup

┌──────────────────────────────────────────────────────────────────────────┐
│ ✅ DONE! Now Test It                                                     │
└──────────────────────────────────────────────────────────────────────────┘

Test #1: Block a user in your database
  1. Go to User Management in your app
  2. Click "Deactivate" on a test user
  3. Try to login with that user
  4. Should see: "Your account has been blocked"

Test #2: Check database
  1. Open MongoDB
  2. Find the user in "users" collection
  3. Verify: isActive = false ✓

Test #3: Unblock the user
  1. Click "Activate" in User Management
  2. Try to login again
  3. Should succeed ✓

┌──────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                          │
└──────────────────────────────────────────────────────────────────────────┘

Problem: "Action not blocking users"
Solution:
  - Verify Action is in Login Flow (step 7)
  - Check API_BASE_URL secret is correct
  - Test endpoint manually:
    curl "http://localhost:7000/api/admin/users/check-block-status?auth0Id=test"

Problem: "Cannot find action in right sidebar"
Solution:
  - Make sure you clicked Deploy (step 6)
  - Refresh the page
  - Action should appear in "Custom" section

Problem: "Login fails with error"
Solution:
  - Go to: Monitoring → Logs in Auth0 Dashboard
  - Look for errors in the action execution
  - Check backend is running (npm run dev)

┌──────────────────────────────────────────────────────────────────────────┐
│ VERIFICATION CHECKLIST                                                   │
└──────────────────────────────────────────────────────────────────────────┘

After setup, verify:
  □ Action appears in Actions → Library
  □ Action shows "Deployed" status (not "Draft")
  □ Secret API_BASE_URL is set
  □ Action is in Login Flow (Actions → Flows → Login)
  □ Backend endpoint responds:
    GET /api/admin/users/check-block-status?auth0Id=test
  □ Blocked user cannot login
  □ Active user can login

┌──────────────────────────────────────────────────────────────────────────┐
│ WHAT HAPPENS ON EVERY LOGIN                                             │
└──────────────────────────────────────────────────────────────────────────┘

1. User enters email + password
2. Auth0 validates credentials
3. ⚡ YOUR ACTION RUNS HERE ⚡
   → Calls: GET your-api.com/check-block-status?auth0Id=...
   → Your backend checks MongoDB: user.isActive?
   → Returns: { isBlocked: true/false }
4. If blocked → Login DENIED ❌
5. If active → Login ALLOWED ✅

This ensures database blocking takes immediate effect!

╔══════════════════════════════════════════════════════════════════════════╗
║  Need more help? See SETUP_INSTRUCTIONS.md for detailed guide           ║
╚══════════════════════════════════════════════════════════════════════════╝
